const quarterValue = (findQuarter) => {
    const quarter = findQuarter / 4
    return quarter

}

const returnedQuarter = quarterValue(24)
console.log(returnedQuarter)

// Invoke the function and store the return value


// Log the return value